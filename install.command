#!/usr/bin/env bash
cd "$(dirname "$0")" || exit

cp -rip Import\ into\ Resolve.workflow ~/Library/Services/Import\ into\ Resolve.workflow
cp -rip resolve_auto_import ~/Library/Services/resolve_auto_import